#include<stdio.h>
int fun(int [],int);
void main()
{
    int mark[5]={99,98,95,96,100},size,avg;
    size=sizeof(mark)/sizeof(mark[0]);
    avg=avg(mark,size);
    printf("Average = %d",avg);
}
int fun(int mark[],int size)
{
    int sum=0,i,avg=0;
    for(i=0;i<size;i++)
    {
        sum
    }
}
